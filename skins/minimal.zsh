# minimal — a single, spare line: path, git, then a lone caret. No status line,
# no right prompt, no second row. Proof a skin can collapse to one line.
PROMPT_KRONUZ_PROMPT='$kronuz[pwd]$kronuz[git] ${fcol[magenta]}${glyph[caret]}${fcol[none]} '
PROMPT_KRONUZ_RPROMPT=''
PROMPT_KRONUZ_TRANSIENT_PROMPT='$kronuz[pwd] ${fcol[magenta]}${glyph[caret]}${fcol[none]} '
