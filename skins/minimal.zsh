# minimal — a single, spare line: path, git, then a lone caret. No status line,
# no right prompt, no second row. Proof a skin can collapse to one line.
PROMPT_KRONUZ_PS1='$kronuz[pwd]$kronuz[git] ${col[magenta]}${glyph[caret]}${col[none]} '
PROMPT_KRONUZ_RPS1=''
PROMPT_KRONUZ_TRANSIENT='$kronuz[pwd] ${col[magenta]}${glyph[caret]}${col[none]} '
